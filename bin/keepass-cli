#!/usr/bin/env perl

# PODNAME: keepass-cli
# ABSTRACT: utility to manipulate keepass2 file

use strict;
use warnings;
# VERSION
use App::KeePass;

my $keepass = App::KeePass->new_with_options;
$keepass->run;

